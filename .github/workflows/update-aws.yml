on:
  push:
    branches:
      - master
jobs:
  aws_cdk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install CDK and typescript
        run: mkdir /home/runner/.npm-global && npm install typescript && npm install -g aws-cdk
        env:
          NPM_CONFIG_PREFIX: /home/runner/.npm-global
      - name: Show diff
        run: PATH=$PATH:/home/runner/.npm-global/bin cd cdk-k3s/ && cdk diff
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: 'eu-central-1'
          NPM_CONFIG_PREFIX: /home/runner/.npm-global
      - name: Deploy changes to AWS
        run: PATH=$PATH:/home/runner/.npm-global/bin cd cdk-k3s/ && cdk deploy --require-approval never
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: 'eu-central-1'
          NPM_CONFIG_PREFIX: /home/runner/.npm-global
